import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import '@safari/ui/src/app/globals.css'
import { ApolloProvider } from '@safari/network/src/config/apollo'
import { SessionProvider } from '@safari/ui/src/components/molecules/SessionProvider'
import { Header } from '@safari/ui/src/components/organisms/Header'
import { ToastContainer } from '@safari/ui/src/components/molecules/Toast'
import { MenuItem } from '@safari/util/types'
import { Container } from '@safari/ui/src/components/atoms/Container'
const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'H@rtman',
  description: 'Generated by create next app',
}
// HomePage.tsx
const MENUITEMS: MenuItem[] = [
  { label: 'Search', href: '/search' },
  { label: 'Bookings', href: '/bookings' },
  { label: 'Safari', href: '/safaris' },
  {label:'Home', href:'/home'}
]

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <SessionProvider>
        <ApolloProvider>
          <body className={`${inter.className} bg-gray-25`}>
            <Header menuItems={MENUITEMS} />
            <Container>{children}</Container>
            <ToastContainer />
          </body>
        </ApolloProvider>
      </SessionProvider>
    </html>
  )
}
